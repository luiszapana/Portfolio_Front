export const environment = {
    production: false,
    firebaseConfig: {
      //Este contenido es redundante ya que la configuración de firebase quedo establecida en el archivo app.module.ts
      //en el método initializeApp
    }
  };